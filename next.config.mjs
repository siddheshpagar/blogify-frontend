/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost','s3-alpha-sig.figma.com','blogify-backend-ddr6.onrender.com'],
    },
  };
// const nextConfig = {};

export default nextConfig;
